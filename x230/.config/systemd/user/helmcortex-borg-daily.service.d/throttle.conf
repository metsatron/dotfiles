# Stopgap drop-in deployed 2026-06-26 pending `git pull && loom stow:x230` of the
# throttled units from DotCortex services.org. Mirrors the [Service] throttle in source.
[Service]
Nice=19
CPUSchedulingPolicy=idle
IOSchedulingClass=idle
CPUWeight=20
CPUQuota=100%
