#!/usr/bin/env bash
# [[file:../agents-hooks.org::*Git hook wrappers][Git hook wrappers:3]]
#!/usr/bin/env bash
# Rule 21 at publication time: nothing a push would publish may add a network address.
set -euo pipefail
repo="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
address_guard="${HOME}/.local/bin/dotcortex-address-guard"
[ -x "$address_guard" ] || address_guard="$repo/all/.local/bin/dotcortex-address-guard"
exec "$address_guard" --pre-push "$@"
# Git hook wrappers:3 ends here
