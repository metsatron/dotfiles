#!/usr/bin/env bash
# Apply script

# [[file:../../../dnf.org::*Apply script][Apply script:1]]
set -euo pipefail

MAN="${DNF_SSV:-$HOME/.dnf/manifest/packages.ssv}"
SCOPE="${DNF_SCOPE:-openmandriva}"
UPDATE_FIRST="${UPDATE_FIRST:-1}"

if [ ! -f "$MAN" ]; then
  echo "No manifest at $MAN" >&2
  exit 1
fi

if ! command -v dnf >/dev/null 2>&1; then
  echo "dnf not found; this manifest is for OpenMandriva T480" >&2
  exit 1
fi

scope_matches() {
  local scope="$1"
  case "$scope" in
    "$SCOPE"|shared|"") return 0 ;;
    *) return 1 ;;
  esac
}

pkg_available() {
  local pkg="$1"
  dnf -q repoquery "$pkg" >/dev/null 2>&1 || dnf -q repoquery --whatprovides "$pkg" >/dev/null 2>&1
}

pkg_installed() {
  rpm -q "$1" >/dev/null 2>&1
}

INSTALL_LIST=()
SKIPPED=()

while IFS='|' read -r pkg _ver _cat scope _extra; do
  [ -n "$pkg" ] || continue
  scope_matches "$scope" || continue
  pkg_installed "$pkg" && continue
  if pkg_available "$pkg"; then
    INSTALL_LIST+=("$pkg")
  else
    SKIPPED+=("$pkg")
  fi
done < <(
  awk -v FS='[ \t]+' '
    function dq(s){ return (s == "\"\"" ? "" : s) }
    /^[ \t]*#/ || NF==0 { next }
    {
      printf "%s|%s|%s|%s|%s\n",
        dq($1), dq($2), dq($3), dq($4), dq($5)
    }
  ' "$MAN"
)

if [ ${#SKIPPED[@]} -gt 0 ]; then
  echo "Unavailable package names skipped:"
  printf "  ! %s\n" "${SKIPPED[@]}"
fi

if [ ${#INSTALL_LIST[@]} -eq 0 ]; then
  echo "All available DNF manifest packages are installed."
  exit 0
fi

if [ "$UPDATE_FIRST" = "1" ]; then
  sudo dnf --refresh distro-sync -y
fi

echo "Installing ${#INSTALL_LIST[@]} DNF packages..."
printf "  + %s\n" "${INSTALL_LIST[@]}"
sudo dnf --refresh install -y "${INSTALL_LIST[@]}"
# Apply script:1 ends here
