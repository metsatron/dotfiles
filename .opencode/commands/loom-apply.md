# /loom-apply - Structured DotCortex Loom Command

Use this when the task explicitly asks for a Loom verb, tangle, stow, or package application.

Run a loom verb, for example:

```bash
cd ~/DotCortex && loom $ARGUMENTS
```

Report errors and changed files only. No commentary.
