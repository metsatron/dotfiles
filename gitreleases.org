#+TITLE: Git Releases Retired
#+DESCRIPTION: Retired generic GitHub release automation. Package-specific installs now live in the appropriate manifest files.
#+AUTHOR: Mètsàtron — Sovereign Weaver
#+LANGUAGE: en
#+PROPERTY: header-args :mkdirp yes :results silent

This file is intentionally retired.

The former generic `gitrelease` layer was migrated into the package-specific
manifests that own each install path. The `codex-desktop-linux` source install
now lives in `bun.org` under the Bun source-build manifest.

Do not add new release-backed install logic here.
