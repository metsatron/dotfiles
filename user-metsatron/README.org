#+TITLE: user-metsatron (generated by layers.org)

Mètsàtron's user layer (active). Content moves here out of all/ one
module at a time: B1 (2026-09-23) moved .config/git/config. Files that
still have no org source (like that one) stay static here, as they were
in all/; files with an org source are tangled from it.
