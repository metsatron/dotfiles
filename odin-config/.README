# AYN Odin Lite RetroArch configuration

This directory is the Odin's versioned RetroArch configuration. Run
deploy.sh on the host with the device tethered; it deploys the config over
adb (currently from the T480s). This directory is not stowed.
