* text=auto
*.png -text
*.jpg -text
*.ico -text
*.svg text eol=lf
*.theme text eol=lf
# IceWM theme payloads are vendored historical artwork and metadata. Preserve
# their native bytes; whitespace cleanup would alter the source artifacts.
linux/.icewm/themes/** -whitespace
