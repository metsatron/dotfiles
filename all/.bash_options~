# ~/.bash_options
# --------------------------------------------------
# Mètsàtron's Options
# --------------------------------------------------

# append to the history file, don't overwrite it
shopt -s histappend

# Check window size
shopt -s checkwinsize

# Globstar (currently commented out)
#shopt -s globstar
