;; Emacs Config Snippet
;; Created: <DATE>
;; Author: <Your Name>

(use-package some-package
  :ensure t
  :config
  (setq some-variable t))

;; Optional custom function
(defun my-custom-function ()
  "Describe what this function does."
  (interactive)
  (message "My custom function ran."))

