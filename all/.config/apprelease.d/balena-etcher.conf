NAME="balenaEtcher"
FORGE="dead"
APPIMG_NAME="balenaEtcher"
NOTE="balena dropped AppImage support; GitHub releases v2.1.4+ have no AppImage"
# balenaEtcher-2.1.3-x64.AppImage is the last AppImage version
# Current releases (v2.1.4+) only have .deb, .rpm, .dmg, .exe, .zip
# balena's own download server no longer hosts AppImages
# If you need a newer version, use the .deb or Flatpak instead.
# Move to dead-overrides once the installed AppImage is removed.
