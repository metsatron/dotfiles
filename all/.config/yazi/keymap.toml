# Intentionally empty: vim-style navigation is Yazi's compiled-in default.
# Add narrow [[mgr.prepend_keymap]] / [[input.prepend_keymap]] overrides here
# only once a real gap is found against the installed version — do not
# invent bindings speculatively.
