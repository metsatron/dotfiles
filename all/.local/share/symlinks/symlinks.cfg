# Where logs live
log_dir=/home/metsatron/.local/share/symlinks
# 1 = make timestamped logs (...-YYYYMMDD-HHMMSS.log) + symlink latest.log
# 0 = append to a single broken-symlinks.log
timestamp_logs=1

# Blacklist some noisy subtrees
exclude_dir=/home/metsatron/.local/share/Trash

# Optional: if you know old HOME embedded in icon packs/themes
# (helps convert /home/ander/... -> $HOME/...)
old_home=/home/ander
