#!/usr/bin/env bash
# Health

# [[file:../../../cargo.org::*Health][Health:1]]
set -euo pipefail
"$HOME/.local/bin/dotcortex-rust-env" bash -lc '
echo "RUSTUP_HOME=$RUSTUP_HOME"
echo "CARGO_HOME=$CARGO_HOME"
command -v cargo; cargo --version
command -v rustc; rustc --version
'
# Health:1 ends here
