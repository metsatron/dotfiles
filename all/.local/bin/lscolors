#!/usr/bin/env bash
set -euo pipefail
cols=$(( $(tput cols 2>/dev/null || echo 80) - 4 ))
printf -v s "%${cols}s"
max=$(tput colors 2>/dev/null || echo 8)
for ((i=0;i<=max;i++)); do
  printf "%03d " "$i"
  tput setaf "$i"; tput setab "$i"
  echo "${s// /=}"
  tput sgr0
done