#!/usr/bin/env bash
# Usage: nvide-app astronvim [extra neovide args...]
res="$(xrandr | awk '/\*/{print $1; exit}')"   
profile="${1:-astronvim}"
shift || true
export NVIM_APPNAME="$profile"
exec neovide --size="$res" "$@"
