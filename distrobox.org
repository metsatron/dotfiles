#+TITLE: Virtual Habitat — Distrobox Rooms
#+DESCRIPTION: Distrobox room declarations for the three Phase 1 desktop rooms
#+AUTHOR: Mètsàtron — Sovereign Weaver
#+LANGUAGE: en
#+PROPERTY: header-args :mkdirp yes :results silent
#+PROPERTY: header-args:bash :comments both

* Purpose

Declares the three Phase 1 Distrobox desktop rooms. Each room gets its own Xephyr
display, KWin virtual desktop, DotCortex-tangled home, and a read-only bind-mount
of the host =/gnu/store=.

Rooms are created with =distrobox-rooms-create= (tangled below). This is a one-shot
declarative script — to recreate a room: =distrobox rm <room> && distrobox-rooms-create=.

* Phase 1A: room-qtile — Emacs Sanctuary

Stack: Guix-backed Distrobox + Qtile (Python WM) + multi-process Emacs.
Base image: TBD — candidate is a minimal Debian/Ubuntu base with Guix store projected in.
See =Guix-Package-Commons.md= for the profile composability design.

* Phase 1A: room-gnustep — GNU/NeXT Lineage Room

Stack: Window Maker + GNUstep.
Base image: TBD — candidate is Debian stable (apt-installable: =windowmaker=,
=gnustep-make=, =gnustep-base=, =gnustep-gui=).

* Phase 1B: room-cde — Motif/Sun Room

Stack: Open CDE compiled from source, pinned as git input.
Base image: TBD — needs build toolchain + Motif/X11 deps. Candidate: Debian stable.
Source: =github.com/arcfide/CDesktopEnv= or =cdesktopenv.sourceforge.io=.

* Room Create Script

Fill in image names before Phase 1A build begins. Uncomment each block when ready.

#+BEGIN_SRC bash :tangle linux/.local/bin/distrobox-rooms-create :shebang "#!/usr/bin/env bash"
# Create the three Phase 1 Distrobox desktop rooms.
# Run once per machine, or after `distrobox rm <room>` to recreate.
#
# Fill in image variables below before Phase 1A build begins.
# Guest homes are host-visible: $GUESTS_DIR/<room>/home/
set -euo pipefail

GUESTS_DIR="${XDG_DATA_HOME:-$HOME/.local/share}/dotcortex/guests"
GUIX_PROFILES="$HOME/.guix-extra-profiles"

# --- room-qtile: Emacs Sanctuary ---
# Base image TBD: minimal Debian/Ubuntu base + Guix store projected in
# ROOM_QTILE_IMAGE=""
#
# distrobox create \
#   --name room-qtile \
#   --image "$ROOM_QTILE_IMAGE" \
#   --home "$GUESTS_DIR/room-qtile/home" \
#   --volume /gnu/store:/gnu/store:ro \
#   --volume "$GUIX_PROFILES:$GUIX_PROFILES:ro" \
#   --no-entry

# --- room-gnustep: GNU/NeXT Lineage Room ---
# Base image TBD: Debian stable (windowmaker, gnustep-* from apt)
# ROOM_GNUSTEP_IMAGE=""
#
# distrobox create \
#   --name room-gnustep \
#   --image "$ROOM_GNUSTEP_IMAGE" \
#   --home "$GUESTS_DIR/room-gnustep/home" \
#   --volume /gnu/store:/gnu/store:ro \
#   --no-entry

# --- room-cde: Motif/Sun Room (Open CDE from source) ---
# Base image TBD: Debian stable + build toolchain + Motif/X11 deps
# ROOM_CDE_IMAGE=""
#
# distrobox create \
#   --name room-cde \
#   --image "$ROOM_CDE_IMAGE" \
#   --home "$GUESTS_DIR/room-cde/home" \
#   --no-entry

echo "distrobox-rooms-create: all room stubs are commented out — fill in image names first"
#+END_SRC

* Common Infrastructure Helper

#+BEGIN_SRC bash :tangle linux/.local/bin/distrobox-room :shebang "#!/usr/bin/env bash"
# Wrapper for common distrobox room operations.
# Usage: distrobox-room <verb> [room-name]
#   verbs: list, enter, stop, create
set -euo pipefail

VERB="${1:-list}"
ROOM="${2:-}"

case "$VERB" in
  list)
    distrobox list
    ;;
  enter)
    [[ -z "$ROOM" ]] && { echo "Usage: distrobox-room enter <room-name>"; exit 1; }
    distrobox enter "$ROOM"
    ;;
  stop)
    [[ -z "$ROOM" ]] && { echo "Usage: distrobox-room stop <room-name>"; exit 1; }
    distrobox stop "$ROOM"
    ;;
  create)
    distrobox-rooms-create
    ;;
  *)
    echo "Unknown verb: $VERB"
    echo "Valid verbs: list, enter, stop, create"
    exit 1
    ;;
esac
#+END_SRC
