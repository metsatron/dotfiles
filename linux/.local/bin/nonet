#!/usr/bin/env bash
set -euo pipefail
exec bwrap --unshare-net --dev-bind / / \
  --setenv HOME "$HOME" \
  --setenv PATH "$PATH" \
  --setenv DISPLAY "${DISPLAY:-}" \
  --setenv XAUTHORITY "${XAUTHORITY:-$HOME/.Xauthority}" \
  -- "$@"
