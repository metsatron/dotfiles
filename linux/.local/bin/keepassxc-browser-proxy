#!/usr/bin/env bash
set -euo pipefail
# Native Messaging wrapper that a Flatpak Firefox-family browser can exec.
# It launches the Flatpak KeePassXC proxy inside the KeePassXC sandbox.
exec flatpak-spawn --host flatpak run --command=keepassxc-proxy org.keepassxc.KeePassXC
