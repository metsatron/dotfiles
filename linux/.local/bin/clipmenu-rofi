#!/usr/bin/env bash
P="$HOME/.guix-extra-profiles/core/core"; [ -r "$P/etc/profile" ] && . "$P/etc/profile"
[ -d "$P/lib/locale" ] && export GUIX_LOCPATH="$P/lib/locale"
set -euo pipefail
export CM_LAUNCHER=rofi
exec clipmenu
