#!/usr/bin/env bash
set -euo pipefail

PCLIP="$HOME/HelmCortex/FORGE/VoxForge/bin/pclip"

if [ ! -x "$PCLIP" ]; then
  printf 'pclip-default: missing pclip: %s\n' "$PCLIP" >&2
  exit 1
fi

exec "$PCLIP" default "$@"
