# Measured normal RetroArch path: threaded presentation with synchronized
# video and audio.  This file is appended at launch and never replaces the
# room's mutable retroarch.cfg.
video_threaded = "true"
video_vsync = "true"
video_swap_interval = "1"
audio_sync = "true"
